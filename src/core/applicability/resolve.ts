import type { DocumentSchema, ApplicabilityPolicy } from "../schema/types.js";
import type { CapabilityReport, DocumentApplicability } from "./types.js";
export function resolveApplicability(schema: DocumentSchema, report: CapabilityReport): DocumentApplicability { const configured = schema.applicability ?? "required"; if (typeof configured === "string") return { document: schema.path, state: configured === "conditional" ? "not_applicable" : configured, reasons: [] }; const policy = configured as ApplicabilityPolicy; const matched = (policy.when_any ?? []).filter((capability) => report.capabilities.includes(capability)); return { document: schema.path, state: matched.length ? policy.present : policy.absent, reasons: matched.flatMap((capability) => report.reasons[capability] ?? []) }; }
